?? abline
require(lattice)
require(mosaic)
require(RColorBrewer)
require(vcd)
load('~/Dropbox/IDS Curriculum Design/Folder-James/Unit 1/Data/cdc.rda')
View(cdc)
histogram(~height, data=cdc)
abline(v = mean(cdc$height, na.rm = TRUE), col = "red")
histogram(~height, data=cdc)
abline(v = mean(cdc$height, na.rm = TRUE), col = "red")
hist(cdc$weight)
abline(v = mean(cdc$weight, na.rm = TRUE), col = "red")
hist(cdc$height)
abline(v = mean(cdc$height, na.rm = TRUE), col = "red")
barchart(xtabs(~ depressed +describe_weight, data=cdc),horizontal=F)
plot(general_health~depressed, data=cdc)
plot(depressed~general_health, data=cdc)
barchart(xtabs(~ depressed +describe_weight, data=cdc),horizontal=F)
plot(weight~depressed, data=cdc)
mosaic(gender~ depressed, data=cdc)
mosaic(depressed~gender, data=cdc)
mosaic(depressed~gender, horizontal=F, data=cdc)
mosaic(gender~ depressed,horizontal=F, data=cdc)
mosaic(depressed~gender, horizontal=F, data=cdc)
mosaic(gender~ depressed,horizontal=F, data=cdc)
mosaic(gender~ depressed, data=cdc)
mosaic(gender~ depressed, data=cdc)
load('~/Dropbox/IDS Curriculum Design/Folder-James/Unit 1/Data/cdc.rda')
View(cdc)
mosaic(gender~ depressed, data=cdc)
mosaic(depressed~gender, data=cdc)
mosaic(gender~ depressed, data=cdc)
mosaic(depressed~gender, data=cdc)
histogram(~weight[cdc$gender == "Female"],data=cdc)
abline(v=mean(weight[cdc$gender =="Female"]))
abline(v=mean(weight[cdc$gender =="Female"]), na.rm=T)
abline(v=mean(cdc$weight[cdc$gender =="Female"]), na.rm=T)
abline(v=mean(cdc$weight[cdc$gender =="Female"]))
hist(cdc$weight[cdc$gender == "Female"])
abline(v=mean(cdc$weight[cdc$gender =="Female"]))
abline(v=mean(cdc$weight[cdc$gender =="Female"]))
abline(v=mean(cdc$weight[cdc$gender =="Female"]),colore="red")
abline(v=mean(cdc$weight[cdc$gender =="Female"]),col="red")
abline(v=mean(cdc$weight[cdc$gender =="Female"]),col="red")
hist(cdc$weight[cdc$gender == "male"])
```
hist(cdc$weight[cdc$gender == "male"])
hist(cdc$weight[cdc$gender == "Male"])
abline(v=mean(cdc$weight[cdc$gender =="Male"]),col="red")
hist(cdc$weight[cdc$gender == "Male"])
abline(v=mean(cdc$weight[cdc$gender =="Male"]),col="red")
hist(cdc$weight[cdc$gender == "Female"])
abline(v=mean(cdc$weight[cdc$gender =="Female"]),col="blue")
hist(cdc$weight[cdc$gender == "Male"])
abline(v=mean(cdc$weight[cdc$gender =="Male"]),col="red")
abline(v=mean(cdc$weight[cdc$gender =="Male" &cdc$height > 1.83]),col="red")
hist(cdc$weight[cdc$gender == "Female" & cdc$height >1.83])
hist(cdc$weight[cdc$gender == "Male"])
hist(cdc$weight[cdc$gender == "Female" & cdc$height >1.83])
hist(cdc$weight[cdc$gender == "Female" & cdc$height >1.63])
mosaicplot(table(cdc$gender, cdc$eat_fruit))
mosaicplot(table(cdc$gender, cdc$eat_fruit), las=2)
library("knitr", lib.loc="/Library/Frameworks/R.framework/Versions/2.14/Resources/library")
Lab 1.1
install.packages("knitr")
library("knitr", lib.loc="/Library/Frameworks/R.framework/Versions/3.0/Resources/library")
